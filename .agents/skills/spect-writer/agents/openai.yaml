interface:
  display_name: "Spec Writer"
  short_description: "Initialize or refresh project overview documentation"
  default_prompt: "Use $spect-writer init-project to refresh the compact project purpose, stack, goals, and main areas in docs/project-overview.md. For feature/fix specs, use /autopilot spec instead."
