# Summary Action

Gather active spec goals plus `git status` and relevant `git diff`.

Output:

```markdown
## Summary
## Goals Status
## Files Changed
## Tests
## Notes / Risks
## Next Step
```

Also check: no unrelated files, no secrets, no unnecessary dependency, migrations reviewed, ApiResponse used for HTTP JSON, no obvious N+1 risk.
