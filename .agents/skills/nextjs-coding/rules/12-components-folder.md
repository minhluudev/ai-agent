# Rule 12 - `components/<component-name>/`

## Purpose
Reusable UI for multiple containers/routes.

## Must
- Use `.agents/skills/react-component-generator/SKILL.md` when creating/refactoring components.
- Keep components generic and reusable across features.

## Must Not
- Depend on one specific container/controller.
- Contain feature orchestration.
